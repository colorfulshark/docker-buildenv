SCRIPTPATH=${SCRIPTPATH:-$(dirname $BASH_SOURCE)}
SCRIPTNAME=${SCRIPTNAME:-$(basename $BASH_SOURCE)}

# docker image id or name
image=''

# container name
name=$SCRIPTNAME

# your workspcae directory
workspace=`pwd`

# volume mount arguments
volume=""

# extra arguments
extra_option=""

# extra command, this command will be executed in
# container as root user
extra_cmd=""

# this function will be called before container created
function init_project() {
	echo 'init project'
}
